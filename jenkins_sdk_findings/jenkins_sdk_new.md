 Новые находки подтверждают и расширяют предыдущие выводы. Главное — это переход от ручного написания кода к автоматической генерации через макросы и спецификации.

 ### ✨ Что мы знаем (External Evidence - Новые инструменты)

 1. Автоматический Генератор Спецификаций Jenkins API: Существуют проекты, которые могут сканировать сам код Jenkins API и генерировать документацию/спецификации
    (похоже на OpenAPI/Swagger). Это позволяет нам иметь "источник правды" для наших 35 методов.
 2. robusta_jni / jni-utils: Эти крейты предоставляют процедурные макросы. Вместо того чтобы писать:
    ```rust
      #[no_mangle]
      pub extern "C" fn fetch_pull_requests(repo_id: i32, token: *const c_char) -> JString { /* ... */ }
    ```
    Вы можете написать что-то вроде:
    ```rust
      #[derive(JniExport)] // или аналогичный макрос из jni-utils
      pub struct GiteaClient { /* ... */ }

      impl GiteaClient {    
          #[jni_export] // Или #[no_mangle] с атрибутом, который генерирует остальное
          fn fetch_pull_requests(&self, repo_id: i32, token: &str) -> Result<Vec<PullRequest>, GiteaError> { /* ... */ }
      }
    ```
    Макрос сам позаботится о создании всех необходимых extern "C" функций и правильном преобразовании типов (например, из Rust String в Java JString).

 ### 💻 Что подразумевает локальный код (Local Code Context)

 - Контекст: Наш текущий подход — это ручное написание 35 методов с явными #[no_mangle] и ручным управлением типами.
 - Точка входа для ускорения: Макросы (robusta_jni / jni-utils) идеально подходят для автоматизации Этапа 2 (JNI exports), который является самой трудоемкой частью
   нашей текущей реализации.

 ### ⚖️ Компромиссы и Риски (Tradeoffs & Risks)

 ┌──────────────────┬──────────────────────────────────────┬───────────────────────────────────────┬────────────────────────┬───────────────────────────────────────┐
 │ Стратегия        │ Основной фокус ускорения             │ Риск внедрения                        │ Стоимость поддержки    │ Примечание                            │
 ├──────────────────┼──────────────────────────────────────┼───────────────────────────────────────┼────────────────────────┼───────────────────────────────────────┤
 │ SDK + Макросы    │ Автоматизация вызовов (через SDK) И  │ Низкий/Средний. Нужно настроить оба   │ Низкая. Код становится │ Наиболее мощный вариант.              │
 │                  │ автоматизация оберток (через         │ инструмента.                          │ декларативным.         │                                       │
 │                  │ макрос).                             │                                       │                        │                                       │
 ├──────────────────┼──────────────────────────────────────┼───────────────────────────────────────┼────────────────────────┼───────────────────────────────────────┤
 │ Только SDK       │ Автоматизация вызовов.               │ Средний. Требует ручного написания    │ Низкая.                │ Отлично, если не хочется возиться с   │
 │                  │                                      │ адаптеров для 35 методов.             │                        │ JNI-макросами.                        │
 ├──────────────────┼──────────────────────────────────────┼───────────────────────────────────────┼────────────────────────┼───────────────────────────────────────┤
 │ Только Макросы   │ Автоматизация оберток.               │ Средний. Нужно вручную писать логику  │ Средняя.               │ Хорошо, если SDK окажется слишком     │
 │ (с reqwest)      │                                      │ HTTP/пагинации для каждого метода.    │                        │ "тяжелым" или не покрывает специфику  │
 │                  │                                      │                                       │                        │ Gitea.                                │
 └──────────────────┴──────────────────────────────────────┴───────────────────────────────────────┴────────────────────────┴───────────────────────────────────────┘

 ### ❓ Пробелы или Допущения (Gaps/Assumptions)

 - Допущение: Мы предполагаем, что robusta_jni или аналогичный макрос корректно обрабатывают сложные типы данных Rust $\leftrightarrow$ Java (например, Result<T, E>
   в виде Java Optional<T>).
 - Пробел: Нам нужно проверить, как эти инструменты работают с асинхронностью.

 ### 🚀 Рекомендуемый следующий шаг (Recommended Next Move)

 Принять гибридный подход, основанный на Макросах:
 1. Использовать jenkins-sdk для определения что мы делаем (какой сервис и какой метод вызываем).
 2. Использовать крейт вроде robusta_jni для автоматической генерации JNI-оберток, которые вызывают методы из jenkins-sdk.

 Это даст нам максимальную скорость разработки при минимальном риске нарушения архитектуры!<|"|>,oldText:<|"|>## 🛠️ Дополнительные инструменты и паттерны (Ускорение)

 Новые находки подтверждают и расширяют предыдущие выводы. Главное — это переход от ручного написания кода к автоматической генерации через макросы и спецификации.

 ### ✨ Что мы знаем (External Evidence - Новые инструменты)

 1. Автоматический Генератор Спецификаций Jenkins API: Существуют проекты, которые могут сканировать сам код Jenkins API и генерировать документацию/спецификации
    (похоже на OpenAPI/Swagger). Это позволяет нам иметь "источник правды" для наших 35 методов.
 2. robusta_jni / jni-utils: Эти крейты предоставляют процедурные макросы. Вместо того чтобы писать:
    ```rust
      #[no_mangle]
      pub extern "C" fn fetch_pull_requests(repo_id: i32, token: *const c_char) -> JString { /* ... */ }
    ```
    Вы можете написать что-то вроде:
    ```rust
      #[derive(JniExport)] // или аналогичный макрос из jni-utils
      pub struct GiteaClient { /* ... */ }

      impl GiteaClient {
          #[jni_export] // Или #[no_mangle] с атрибутом, который генерирует остальное
          fn fetch_pull_requests(&self, repo_id: i32, token: &str) -> Result<Vec<PullRequest>, GiteaError> { /* ... */ }
      }
    ```
    Макрос сам позаботится о создании всех необходимых extern "C" функций и правильном преобразовании типов (например, из Rust String в Java JString).

 ### 💻 Что подразумевает локальный код (Local Code Context)

 - Контекст: Наш текущий подход — это ручное написание 35 методов с явными #[no_mangle] и ручным управлением типами.
 - Точка входа для ускорения: Макросы (robusta_jni / jni-utils) идеально подходят для автоматизации Этапа 2 (JNI exports), который является самой трудоемкой частью
   нашей текущей реализации.

 ### ⚖️ Компромиссы и Риски (Tradeoffs & Risks)

 ┌──────────────────┬──────────────────────────────────────┬───────────────────────────────────────┬────────────────────────┬───────────────────────────────────────┐
 │ Стратегия        │ Основной фокус ускорения             │ Риск внедрения                        │ Стоимость поддержки    │ Примечание                            │
 ├──────────────────┼──────────────────────────────────────┼───────────────────────────────────────┼────────────────────────┼───────────────────────────────────────┤
 │ SDK + Макросы    │ Автоматизация вызовов (через SDK) И  │ Низкий/Средний. Нужно настроить оба   │ Низкая. Код становится │ Наиболее мощный вариант.              │
 │                  │ автоматизация оберток (через         │ инструмента.                          │ декларативным.         │                                       │
 │                  │ макрос).                             │                                       │                        │                                       │
 ├──────────────────┼──────────────────────────────────────┼───────────────────────────────────────┼────────────────────────┼───────────────────────────────────────┤
 │ Только SDK       │ Автоматизация вызовов.               │ Средний. Требует ручного написания    │ Низкая.                │ Отлично, если не хочется возиться с   │
 │                  │                                      │ адаптеров для 35 методов.             │                        │ JNI-макросами.                        │
 ├──────────────────┼──────────────────────────────────────┼───────────────────────────────────────┼────────────────────────┼───────────────────────────────────────┤
 │ Только Макросы   │ Автоматизация оберток.               │ Средний. Нужно вручную писать логику  │ Средняя.               │ Хорошо, если SDK окажется слишком     │
 │ (с reqwest)      │                                      │ HTTP/пагинации для каждого метода.    │                        │ "тяжелым" или не покрывает специфику  │
 │                  │                                      │                                       │                        │ Gitea.                                │
 └──────────────────┴──────────────────────────────────────┴───────────────────────────────────────┴────────────────────────┴───────────────────────────────────────┘

 ### ❓ Пробелы или Допущения (Gaps/Assumptions)

 - Допущение: Мы предполагаем, что robusta_jni или аналогичный макрос корректно обрабатывают сложные типы данных Rust $\leftrightarrow$ Java (например, Result<T, E>
   в виде Java Optional<T>).
 - Пробел: Нам нужно проверить, как эти инструменты работают с асинхронностью.

 ### 🚀 Рекомендуемый следующий шаг (Recommended Next Move)

 Принять гибридный подход, основанный на Макросах:
 1. Использовать jenkins-sdk для определения что мы делаем (какой сервис и какой метод вызываем).
 2. Использовать крейт вроде robusta_jni для автоматической генерации JNI-оберток, которые вызывают методы из jenkins-sdk.

 Это даст нам максимальную скорость разработки при минимальном риске нарушения